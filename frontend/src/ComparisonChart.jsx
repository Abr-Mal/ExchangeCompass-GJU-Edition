// ComparisonChart.jsx
// The comparison feature was removed. This file now exports a small placeholder component
// so imports won't break if referenced elsewhere.

import React from 'react';

const ComparisonChart = () => {
  return (
    <div style={{ padding: '12px', textAlign: 'center', color: '#666' }}>
      <em>University comparison feature has been removed.</em>
    </div>
  );
};

export default ComparisonChart;